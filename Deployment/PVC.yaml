apiVersion: v1
kind : PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: videoclub
spec:
  accessModes: 
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: sql-storage
